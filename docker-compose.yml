version: '3.8'

services:
  gemini-factory:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./storage:/app/storage
      - ./config.json:/app/config.json
      - ./history.json:/app/history.json
    restart: always
